const EMPTY_ERROR = 'field is mandatory';
const ONLY_STRING_ERROR = 'field should a only alphabets';
const ONLY_DIGITS_ERROR = 'field can only contain numbers';

export default {
    EMPTY_ERROR,
    ONLY_STRING_ERROR,
    ONLY_DIGITS_ERROR,
}
