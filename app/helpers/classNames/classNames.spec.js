import classNames from './classNames';

describe('classnames',() => {
    "use strict";
    it('returns class names as string', () => {
        const result = classNames('test', 'test1');
    });
})
